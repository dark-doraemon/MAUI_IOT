<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MAUI_IOT.Views.LessonView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
    xmlns:viewmodel="clr-namespace:MAUI_IOT.ViewModels"
    Title="LessonView"
    x:DataType="viewmodel:LessonViewModel">

    <ContentPage.BindingContext>
        <viewmodel:LessonViewModel/>
    </ContentPage.BindingContext>


    <ContentPage.Content>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="{Binding Lesson.LessonName}" HorizontalOptions="Center" FontSize="Large"/>
            
            <VerticalStackLayout  Grid.Row="1" Margin="10">
                <HorizontalStackLayout>
                    <Label Text="x" WidthRequest="150"/>
                    <Label x:Name="lbl_x" WidthRequest="150" Text="{Binding ADXL345Axis.x}" />
                </HorizontalStackLayout>

                <HorizontalStackLayout>
                    <Label Text="y" WidthRequest="150"/>
                    <Label x:Name="lbl_y" WidthRequest="150" Text="{Binding ADXL345Axis.y}"/>
                </HorizontalStackLayout>

                <HorizontalStackLayout>
                    <Label Text="z" WidthRequest="150"/>
                    <Label x:Name="lbl_z" WidthRequest="150" Text="{Binding ADXL345Axis.z}"/>
                </HorizontalStackLayout>
                
            </VerticalStackLayout>


            <lvc:CartesianChart Grid.Row="2" Series="{Binding Series}" ZoomMode="Both" ></lvc:CartesianChart>

            <StackLayout Grid.Row="4" Orientation="Horizontal" HorizontalOptions="CenterAndExpand" VerticalOptions="EndAndExpand"  Spacing="30" >  
                <Button Text="Start" Command="{Binding StartCommand}"/>
                <Button Text="Stop"  Command="{Binding StopCommand}"/>
                <Button Text="Save"  Command="{Binding SaveCommand}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>